# Prometheus scrape config for the local compose stack.
global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: dtr-runtime
    static_configs:
      - targets: ["runtime:9090"]
        labels:
          service: dtr-runtime
          env: local
